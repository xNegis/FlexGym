DATABASE_URL=sqlite:///../data/db/flexgym.db
ALLOWED_ORIGINS=http://localhost:5173
APP_ENV=development
JWT_SECRET=flexgym-development-secret-do-not-use-in-production
# Optional private object storage for body-progress photos. Credentials are
# resolved through the normal AWS provider chain (env vars, shared credentials
# file, or instance role) and are never stored in application tables or logs.
# S3_REGION=eu-west-1
# S3_BUCKET=flexgym-private
# S3_PREFIX=body-progress
# BODY_PROGRESS_PHOTO_GLOBAL_LIMIT=10000
